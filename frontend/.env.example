# URL of the deployed Cloudflare Worker.
# Used for: post-registration profile creation, agent registry sync, window commit counts.
# Leave unset in local dev if you don't have a deployed worker — features degrade gracefully.
VITE_WORKER_URL=https://veritas-worker.YOUR_ACCOUNT.workers.dev
